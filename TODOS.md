# TODOS.md — Agent Data Gateway

> 実装済みの項目はチェック、不要になった項目は削除すること。

---

## P1 — ハッカソンデモ後 / MVP リリース前

### OpenAI 互換エンドポイント
- **What**: `/openai/v1/chat/completions` エンドポイントを追加し、OpenAI API 形式のリクエストも同じゲートウェイで中継・記録できるようにする。
- **Why**: LangChain・LlamaIndex・大半の OSS エージェントフレームワークは OpenAI 形式をデフォルト使用。対応することでアドレサブルマーケットが倍になる。
- **Effort**: M（人間: 〜2日 / CC: 〜1時間）
- **Depends on**: Anthropic API 対応が安定してから

### WHY 全文検索（SQLite FTS5）
- **What**: WHY フィールドに SQLite FTS5 インデックスを追加し、ダッシュボードに検索 UI を実装する。
- **Why**: コンプライアンス担当者が「削除と書かれた呼び出しを全部見せて」という用途に応える。
- **Effort**: S（CC: 〜15分）
- **Depends on**: ダッシュボード UI が安定してから

---

## P2 — OSS リリース後

### 開発者デバッグビュー
- **What**: ダッシュボードに「デバッグ」タブを追加。生のリクエスト/レスポンスペア、レイテンシ（ms）、トークン数を表示する。
- **Why**: 開発者がエージェントの動作を確認・デバッグできる。OSS+Developer-led GTM との整合を強化する。
- **Effort**: M（人間: 〜1日 / CC: 〜30分）
- **Depends on**: OpenAI 互換エンドポイント実装後に同時に対応するとよい

---

## P3 — 将来フェーズ（10x ビジョン）

### 意図カテゴリ構造化
- **What**: WHY テキストを LLM で後処理し、意図カテゴリ（データ参照/変更/削除/外部通信）に自動分類する。
- **Why**: コンプライアンスレポートで「削除系操作が X 件」という集計が可能になる。

### リスクスコア
- **What**: tool_use の名前・入力パラメータ・WHY から自動リスクスコアを算出する。

### 暗号署名（改ざん防止）
- **What**: 監査ログに暗号署名を付加し、改ざん不可能な証跡として法的提出可能にする。

### 自動コンプライアンスレポート
- **What**: 個人情報保護法・金融庁ガイドライン対応の定型レポートを自動生成する。

---

## 実装時 推奨（後回しではなく実装中に対応すること）

- [x] SQLite WAL モードを有効化: `PRAGMA journal_mode=WAL`（プロキシ書き込みとダッシュボード読み込みの競合防止）**完了: v0.1.0.0 (2026-05-07)**
- [x] SQLite `timestamp` カラムにインデックス追加（ダッシュボードのソート）**完了: v0.1.0.0 (2026-05-07)**
- [ ] デモ用モック API エンドポイント（`create_issue` / `assign_issue` / `add_comment`）を Docker Compose に含める（Docker Compose サービスは追加済み、ルート未実装）
- [ ] `anthropic-beta` ヘッダを pass-through に含める（拡張思考・プロンプトキャッシュ対応）
- [ ] `metadata.user_id` を WHO の第一ソースとして実装（`X-Agent-ID` は第二ソース）
- [ ] SSE バッファ上限とフラグ閾値を環境変数で制御可能にする
- [ ] Anthropic SSE の `event: message_stop` の正確なフォーマットをドキュメントで確認してからテストフィクスチャを作成する
